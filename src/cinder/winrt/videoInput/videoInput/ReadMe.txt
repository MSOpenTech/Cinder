========================================================================
Static libraries. Project Overview videoInput
========================================================================

This library project videoInput was created automatically by the application wizard.

As part of the project source files are not created.

videoInput.vcxproj
	This is the main project file VC++, generated using an Application Wizard. 
	It contains data on the language version of Visual C++, used to create the file, 
	as well as information about the platforms, configurations, and project 
	features selected with the Application Wizard.

videoInput.vcxproj.filters
	This file filters for projects VC++, created by the Application Wizard. 
	It contains mapping information between files in your project and filters. 
	These mappings are used by the IDE to group files with the same extension 
	in the same node (eg CPP-mapped files with filter "Sources").

/////////////////////////////////////////////////////////////////////////////

Other notes.

With comments «TODO:» Application Wizard designated source code fragments 
that need to be supplemented or changed.

/////////////////////////////////////////////////////////////////////////////


========================================================================
    СТАТИЧЕСКАЯ БИБЛИОТЕКА. Обзор проекта videoInput
========================================================================

Этот проект библиотеки videoInput создан автоматически с помощью мастера приложений.

В составе проекта не созданы исходные файлы.


videoInput.vcxproj
    Это основной файл проекта VC++, создаваемый с помощью мастера приложений. Он содержит данные о версии языка Visual C++, использованной для создания файла, а также сведения о платформах, конфигурациях и функциях проекта, выбранных с помощью мастера приложений.

videoInput.vcxproj.filters
    Это файл фильтров для проектов VC++, созданный с помощью мастера приложений. Он содержит сведения о сопоставлениях между файлами в вашем проекте и фильтрами. Эти сопоставления используются в среде IDE для группировки файлов с одинаковыми расширениями в одном узле (например CPP-файлы сопоставляются с фильтром "Исходные файлы").

/////////////////////////////////////////////////////////////////////////////
Прочие примечания.

С помощью комментариев «TODO:» в мастере приложений обозначаются фрагменты исходного кода, которые необходимо дополнить или изменить.

/////////////////////////////////////////////////////////////////////////////
